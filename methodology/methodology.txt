#-- HOST DISCOVERY --#
Scan the entire subnet for all hosts

1. Ping sweep the subnet
-use disabled port scan (-sn)
-use ICMP requests (-PE, -PP, -PM)
-still need to decide what type of output file will be used

nmap -sn -PE <IP SUBNET> -oA <file_name> # ICMP echo
nmap -sn -PP <IP SUBNET> -oA <file_name> # ICMP timestamp
nmap -sn -PM <IP SUBNET> -oA <file_name> # ICMP netmask


TCP SYN ping (-PS)
-sends an empty TCP packet with the SYN flag set.
-defaults to port 80. Multiple ports are done in parallel.
-if port is closed, the target sends an RST.
-if port is open, the target sends SYN/ACK. The machine running
nmap sends a RST packet whih doesn't complete the 3-way

