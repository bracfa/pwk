#!/usr/bin/bash

<<COMMENT
This script will go through all *.xml files for a single target
and output a file with list of ports that we can use in eyewitness

Needs input of a single IP address
This script will be called from singleTargetScan.sh 
COMMENT


